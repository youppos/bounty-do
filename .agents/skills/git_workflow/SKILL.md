---
name: git_workflow_rule
description: Guidelines on when to commit and merge code in git
---

# Git 工作流规范

## 核心要求
- **不要急着提交代码并合入到 `main` 分支**：在开发新功能或修复 Bug 时，优先在本地测试并确认，除非用户明确要求提交并合入，否则不要主动进行 `git commit` 和 `git merge`。
